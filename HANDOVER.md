# 文法最強王 ― 引き継ぎメモ（HANDOVER）

小学6年生（国語文法が苦手な子）向けの、ブラウザで動く文法学習ゲーム。
**index.html をダブルクリックするだけで動く**（外部ライブラリなし・インストール不要・file:// で動作）。

## 1. ねらい・設計方針
- 文法用語の丸暗記ではなく「見分け方」を身につける。毎問、正解でも「見分け方」、不正解なら「なぜちがうか＋正解の理由」を短く表示。
- 最初はやさしく（名詞 vs 動詞の2択）、章が進むごとに少しずつ難しく。
- ペナルティなし・ゲームオーバーなし。ステージは答え終われば必ずクリア（星は正答率で★〜★★★）。
- まちがえた問題は、同じステージの最後に「もう一度チャレンジ」で再出題（採点なし）＋「にがて問題」に蓄積。
- 💡ヒントは3段階（一般的な見分け方 → その言葉へのヒント → ほぼ答え）。使っても減点なし。
- 📌ボタンでプレイ中いつでも「今回のポイント」を見返せる。📖ことばカード図鑑は常時参照可。
- 分類は学校文法（中学の口語文法）に合わせる。形容動詞・連体詞・自立語/付属語など。

## 2. 章とステージ（全10章・33ステージ）
| 章 | 内容 | ステージ |
|---|---|---|
| 1 ことばの三きょうだい | 名詞・動詞・形容詞 | 1-1 名詞と動詞 / 1-2 動詞と形容詞 / 1-3 三つに仕分け / 1-4 形が変わっても |
| 2 形容詞と形容動詞 | い／だ、〜な テスト | 2-1 / 2-2 / 2-3 |
| 3 文のしくみ | 主語・述語・修飾語 | 3-1 述語 / 3-2 主語 / 3-3 修飾語 / 3-4 述語の品詞 |
| 4 かざる言葉 | 副詞・連体詞 | 4-1 / 4-2 / 4-3 大きい・大きな・大きく / 4-4 文の中で |
| 5 つなぐ言葉・気持ちの言葉 | 接続詞・感動詞 | 5-1 穴うめ / 5-2 / 5-3 文の中で |
| 6 品詞仕分けマスター | 5品詞→8品詞→上級 | 6-1 / 6-2 / 6-3 |
| 7 文の中からさがせ | タップで品詞探し | 7-1 / 7-2 / 7-3 総合（1文で複数） |
| 8 自動詞・他動詞 | が／を | 8-1 / 8-2 / 8-3 |
| 9 くっつく言葉 | 助詞・助動詞 | 9-1 助詞 / 9-2 助動詞の意味 / 9-3 助詞？助動詞？（「ない」チャレンジ含む） |
| 10 文法探偵 | 全単語にラベル | 10-1 短い文 / 10-2 長い文 / 10-3 付属語も |

出題形式: 仕分け(sort) / 2択・3択(choice) / 穴うめ(fill) / 文節タップ(find) / 文法探偵(detect)。
称号は合計の星で決まる（`RANKS` in stages.js）。星：全問正解★★★、75%以上★★、クリア★。

## 3. ファイル構成
```
index.html            画面の器。script の読み込み順に注意（data → core → stages → ui → game → main）
css/style.css         全スタイル（明るい・親しみやすい・幼すぎない。ライトテーマのみ）
js/data/pos.js        品詞・文の成分・自他・接続詞の種類・助動詞の意味・図鑑カード（EXTRA_CARDS）
js/data/words.js      単語バンク（WORDS）、派生名詞、活用形（CONJ_FORMS）、自他ペア（JITA_PAIRS）
js/data/sentences.js  文の素材（SENTENCES：独自記法、下記）
js/data/items.js      手書き問題（4-3 の2択、3-4 述語、5-1 接続詞穴うめ、9-1 助詞、9-2 助動詞、9-3 付属語、「ない」）
js/data/stages.js     章・ステージ定義（CHAPTERS）、STAGE_LIST/STAGE_MAP、RANKS
js/core/util.js       共通関数（shuffle, balancedSample, el, esc, hash…）
js/core/storage.js    localStorage 保存（キー bunpo_saikyoou_v1）
js/core/sound.js      効果音＋BGM（場面 home/play/challenge/result。2セット切りかえ・フェード・自動再生制限の解除）
js/core/explain.js    説明文の生成（reasonOf / notReason / roleReason）← 文章の質はここ
js/core/questions.js  文の解析（parseSentence）、問題オブジェクト生成（Q.*）、出題候補（Pools.*）
js/core/ui.js         画面の描画（ホーム／一覧／紹介／問題／結果／図鑑／モーダル）
js/core/game.js       進行（出題ループ・採点・再挑戦・にがて・保存）
js/main.js            起動。確認用のURLハッシュも処理
assets/sounds/        SE: se_correct / se_wrong / se_select / se_levelup / se_trophy（英語最強王から流用）
                      BGM スタンダード: bgm_home / bgm_training / bgm_battle1 / bgm_result（mp3, 英語最強王から流用）
 assets/sounds/poke/  BGM ポケモン風: poke_home / poke_play / poke_challenge / poke_result（wav, 九九最強王の
                      ポケモンセンター／マサキのもとへ／戦い(VSトレーナー)／勝利 をASCII名に変えて流用）
```

## 4. データの書き方
### 文（sentences.js）
```
{ s: "その/連 犬/名+は/助 とても/副 元気に/形動:元気だ 走る/動。", roles: "修 主 修 修 述", mod: "0>1,2>3,3>4", lvl: 2 }
```
- 文節は半角スペース区切り。文節内の単語は `+` 区切り。単語は `表記/品詞略[:言い切りの形]`。
- 品詞略：名 動 形 形動 副 連 接 感 助 助動。文節末に「、」「。」をつけてよい。
- `roles`（主 述 修 接 独）と `mod`（かざる>かざられる の番号）は3章（主語・述語・修飾語）で使う文だけ。
- `lvl` 1=短い・やさしい 2=ふつう 3=長い。10章の選別に使う。
- find（文節タップ）は「その品詞の自立語が文中に1つだけ」の文から自動生成されるので、同じ品詞が2つある文は自動で除外される。
- 付属語の「だ」は、過去の「た」が濁ったものなら `だ/助動:た`（遊んだ）、断定なら `だ/助動`（本だ）。

### 単語（words.js）
- `WORDS[品詞] = [{ w, ex, k, n, why }]`。`k` は名詞の種類（もの/人/場所/こと/時/数/代）・感動詞の種類。`n` は形容詞・形容動詞の「〜い／〜な」テストに使う名詞。`why` があれば自動生成の説明より優先。
- 形容詞に「暖かい／柔らかい／細かい」のような「〜だ」形もある語は入れない（2-2 の〜な／〜いテストで両方自然になるため）。
- 連体詞・副詞には `ex`（使い方）を必ず入れる（説明文・ヒントに使う）。

### ステージ（stages.js）
- `{ id, title, desc, learn:[カードkey], point:[ポイント文], count, pool: () => Question[] }`。
- `pool` は出題候補の全体。実際の出題は `U.balancedSample(pool, count, q => q.pos)` で答えごとにバランスよく選ぶ。
- 問題IDは内容から決まる（にがて問題の復元に使う）。文や単語を変えるとIDが変わり、古い「にがて」は自動で捨てられる。

## 5. 説明文の仕組み（explain.js）
- `Explain.reasonOf(tok)`：その品詞である理由（正解時・ヒント3）。品詞ごとのテンプレ＋`why` 上書き。
- `Explain.notReason(tok, chosen)`：選んだ品詞ではない理由。`正解>選択` のペア表（例 `形容動詞>副詞`）、無いものは汎用文。
- 文章を直すときはここを直せば全ステージに反映される。

## 5.5 BGM
- 場面キー：home（ホーム・一覧・紹介・図鑑）/ play（1〜5章・8〜9章・にがて）/ challenge（6・7・10章）/ result（結果）。割り当ては `Game.stageBgm()`。
- `Sound.bgmSets` にセットを追加すれば設定画面の切りかえにも自動で出る。`Sound.bgmBase` が場面ごとの基準音量で、設定の音量（0〜1、既定 0.4）をかけて使う。
- ブラウザの自動再生制限で最初は鳴らないことがある → 最初のタップで `pendingKey` の曲を鳴らす（sound.js の unlock）。
- ホーム右上の 🔊BGM ボタンと設定モーダルでオン／オフ。設定は localStorage の settings に保存（bgm / bgmSet / bgmVolume）。

## 6. 動作確認
- Browserペイン：`.claude/launch.json` の "bunpo"（英語最強王側の launch.json にも同名を追加済み）＝ `python -m http.server 8932`。
- URLハッシュで直接ひらける：`#map` `#cards` `#intro=1-1` `#stage=1-1` `#stage=2-1&a=ok|ng`（自動で正解／不正解を押す） `#result=1-3`（結果画面のダミー） `#settings`（設定モーダル）。
- 全問題の整合性チェック（ブラウザのコンソールで）：各ステージ `Game.pool(id)` の全問について `check()` が「正解がちょうど1つ」になることを確認するスクリプトを、開発時に実行済み（ERRORS 0）。
- 自動プレイ回帰：`Game.startStage(id)` → `Game.answer(値)` → `.feedback .btn` をクリック、を全ステージで回して例外なしを確認済み。

## 7. 既知の注意点・今後の案
- 学校文法でも説はゆれる語（こんな・そんな、いっしょに、暖かい系）は素材から外している。追加するときは注意。
- 星は「答え終われば必ず1つ」。厳しくしたい場合は `Storage.calcStars` を変更。
- ステージ解放は直線（前をクリアで次）。設定の「ぜんぶのステージをひらく」で全解放可（保護者用）。
- 今後の拡張候補：文節の区切り問題、敬語、指示語（こそあど）、学校のテスト形式（記述）。
